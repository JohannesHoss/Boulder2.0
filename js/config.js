/**
 * Boulder 2.0 - Runtime Configuration
 *
 * Diese Datei wird bei Docker-Builds ueberschrieben.
 * Der Default-Wert hier gilt fuer Non-Docker-Plattformen (Railway, Render).
 */
window.BOULDER_CONFIG = {
    apiUrl: 'https://boulder-api.varga.media'
};
